{
  "type": "object",
  "title": "extractor configuration",
  "required": [
    "#api_token",
    "endpoint"
  ],
  "properties": {
    "#api_token": {
      "type": "string",
      "title": "API Key",
      "propertyOrder": 100
    },
    "endpoint": {
      "enum": [
        "create_contact",
        "create_list",
        "add_contact_to_list",
        "remove_contact_from_list"
      ],
      "type": "string",
      "title": "Endpoint",
      "propertyOrder": 200,
      "options": {
        "enum_titles": [
          "Create Contact",
          "Create List",
          "Add Contact to List",
          "Remove Contact from List"
        ]
      }
    }
  }
}